{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Least-privilege command access for the settings window.",
  "local": true,
  "windows": ["settings"],
  "permissions": ["group-commands"]
}
